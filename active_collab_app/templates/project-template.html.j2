<html>
<head>
    <meta charset="utf-8">
    <title>Project {{ project_number }}</title>
    <link rel="stylesheet" href="print.css" media="all">

</head>
<body>
<h1>Project No. {{ project_number }} {% if completed_on %} - Completed on: {{ completed_on }} {% endif %}</h1>
<h2>{{ name }}</h2>
<p>{{ body_formatted|safe }}</p>
<table>
    <tr>
        <td>Project ID (internal)</td>
        <td>{{ id }}</td>
    </tr>
    <tr>
        <td>Client Company</td>
        <td>{{ company_id }} - {{ client_company.name }} (is owner: {{ client_company.is_owner }})</td>
    </tr>
    <tr>
        <td>category_id</td>
        <td>{{ category_id }} {% if category %} - {{ category.name }} {% endif %}</td>
    </tr>
    <tr>
        <td>label_id</td>
        <td>{{ label_id }} {% if label %} - {{ label.name }} {% endif %}</td>
    </tr>
    <tr>
        <td>updated_on</td>
        <td>{{ updated_on }}</td>
    </tr>
    <tr>
        <td>Project Email</td>
        <td>{{ email }}</td>
    </tr>

    <tr>
        <td>Created</td>
        <td>{{ created_by_name }} &lt;{{ created_by_email }}&gt; {{ created_on }}</td>
    </tr>
    <tr>
        <td>Completed</td>
        <td>{{ completed_by_name }} &lt;{{ completed_by_email }}&gt; {{ completed_on }}</td>
    </tr>
    <tr>
        <td>Trashed on</td>
        <td>{{ trashed_on }}</td>
    </tr>

</table>

<h3>Attachments:</h3>
<li>
    <ul>Attachment 1</ul>
</li>

<h3>Members:</h3>

<li>
    <ul>{{ members }}</ul>
</li>

<h3>Tasks:</h3>

<li>
    <ul>Task 1</ul>
</li>

</body>
</html>
